import { createStyles } from "@mantine/core";

export const useContactComponentStyle = createStyles((theme) => ({
    Contact_Component__container: {
        display: "flex",
        flexDirection: 'column'
    }
}))